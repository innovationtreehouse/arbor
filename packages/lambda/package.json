{
  "name": "@arbor/lambda",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && node build.mjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@arbor/db": "*",
    "@arbor/logger": "*",
    "@aws-sdk/client-ecs": "^3.0.0",
    "@aws-sdk/client-sqs": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.0.0"
  }
}
