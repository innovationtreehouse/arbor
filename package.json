{
  "name": "arbor",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -p packages/db && tsc -p packages/logger && tsc --build packages/lambda packages/agent packages/mcp-url-fetcher",
    "clean": "rm -rf packages/*/dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:dev": "vitest run --config vitest.integration.config.ts packages/db",
    "test:integration:local": "bash scripts/test-integration.sh"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "aws-sdk-client-mock": "^4.1.0",
    "vitest": "^4.1.0"
  }
}
